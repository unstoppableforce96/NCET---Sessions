/* Program to print the area of a rectangle */
/* length, breadth */
#include <stdio.h>
int main() 
{
	int str[5] = {'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i'};
	for (int i = 0; i < 9; i++) {
		printf("%c ", str[i]);
	}
}