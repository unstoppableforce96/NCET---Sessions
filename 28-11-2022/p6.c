#include <stdio.h>
int main() 
{
	int a = 10, b = 4;
//	printf("%f", a / b);	// multiply any of the values with a 1.0 before performing division
	// explicit type casting
	printf("%f", (float) a / 4);
}