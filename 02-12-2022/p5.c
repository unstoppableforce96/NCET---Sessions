// Find out the largest of two distinct numbers
#include <stdio.h>
int main()
{
	int a, b;
	scanf("%d%d", &a, &b);
	if (a > b) 
	{
		printf("a is the largest");
	}
	else 
	{
		printf("b is the largest");
	}
}